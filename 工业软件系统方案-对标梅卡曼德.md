# 工业软件系统方案（对标梅卡曼德）

## 1. 产品定位
构建一套面向离散制造与智能物流场景的“3D视觉+机器人智能”工业软件平台，覆盖**离线仿真、在线部署、任务编排、质量追溯、运维管理**全流程。

目标是形成与梅卡曼德同级别能力：
- 快速完成3D相机标定与机器人手眼标定；
- 支持复杂工况下的目标识别、抓取位姿估计与路径规划；
- 提供低代码任务配置，缩短项目交付周期；
- 可在多产线、多工位统一运维与持续优化。

---

## 2. 业务场景（优先级）

### P0 场景（首发）
1. **无序抓取（Bin Picking）**
   - 箱内随机堆叠零件抓取；
   - 支持遮挡、反光、相似件混放。
2. **拆码垛/混码垛**
   - 电商仓配与工厂出入库自动化。
3. **上下料**
   - 机床、注塑机、冲压线自动上下料。

### P1 场景（增强）
1. 视觉引导装配定位；
2. 焊缝/涂胶路径引导；
3. AI缺陷检测与追溯联动。

---

## 3. 系统总体架构

```text
┌──────────────────────────────────────────────┐
│                  应用层（Application）        │
│  任务模板中心 | 工艺配置 | 产线看板 | 报表追溯 │
└──────────────────────────────────────────────┘
                    │
┌──────────────────────────────────────────────┐
│               编排层（Orchestration）         │
│ 工作流引擎 | 任务调度 | 异常恢复 | 多工位协同   │
└──────────────────────────────────────────────┘
                    │
┌──────────────────────────────────────────────┐
│              能力层（Intelligence Core）      │
│ 3D视觉感知 | 6D位姿估计 | 抓取规划 | 轨迹规划   │
│ 碰撞检测   | 仿真验证   | 在线学习/参数自适应    │
└──────────────────────────────────────────────┘
                    │
┌──────────────────────────────────────────────┐
│                设备层（Device）               │
│ 3D相机/光源 | 机械臂 | PLC | 夹具 | 传感器      │
│ 支持: OPC UA / Profinet / EtherNet/IP / TCP  │
└──────────────────────────────────────────────┘
```

---

## 4. 核心模块设计

### 4.1 Vision Studio（视觉配置与训练）
- 相机接入、曝光策略、ROI配置；
- 标定向导（内参、外参、手眼）；
- 数据集采集与标注；
- 模型训练、评估与版本管理；
- 一键发布到边缘推理节点。

### 4.2 Robot Runtime（机器人运行时）
- 标准机器人驱动适配（ABB、KUKA、FANUC、Yaskawa、UR）；
- 抓取候选生成与评分；
- 轨迹规划与防碰撞；
- 失败重抓策略（重定位、姿态切换、工艺降级）。

### 4.3 Workflow Composer（低代码任务编排）
- 拖拽式流程图：识别 → 抓取 → 放置 → 质检 → 记录；
- 条件分支与异常回路；
- 工艺参数模板化（按SKU自动切换）。

### 4.4 Ops Center（运维与可观测）
- 工位状态监控（节拍、成功率、OEE）；
- 日志、告警、远程诊断；
- 模型/参数灰度发布与回滚；
- 项目资产管理（模型、流程、配方、权限）。

---

## 5. 技术栈建议

### 边缘端（实时控制）
- C++/Rust（高性能推理与规划）+ Python（快速算法迭代）；
- gRPC/ZeroMQ 用于模块通信；
- ONNX Runtime / TensorRT 加速推理；
- Ubuntu LTS + PREEMPT_RT（可选）满足实时性。

### 平台端（管理与协同）
- 后端：Go/Java + PostgreSQL + Redis + Kafka；
- 前端：Vue/React + WebGL（3D点云可视化）；
- 部署：Kubernetes + Helm；
- 观测：Prometheus + Grafana + Loki。

---

## 6. 数据与模型闭环

1. 现场采集失败样本；
2. 自动入库并标记失败原因（遮挡/反光/碰撞/空抓）；
3. 触发增量训练与回归评测；
4. 小流量工位灰度验证；
5. 达标后全量发布。

关键指标：
- 抓取成功率 ≥ 99%（稳定工况）；
- 单次推理延迟 ≤ 150ms；
- 项目部署周期缩短 30%~50%。

---

## 7. 安全与工业合规
- 网络分区与零信任访问；
- 设备证书与通信加密；
- 操作审计与配置追溯；
- 支持 IEC 62443 思路设计；
- 与功能安全回路（急停/安全门）联动。

---

## 8. 商业化版本规划

### Standard
- 单工位部署；
- 标准无序抓取模板；
- 基础报表与告警。

### Professional
- 多工位调度；
- 高级路径规划与仿真；
- API集成（MES/WMS/ERP）。

### Enterprise
- 集团级多工厂管理；
- 私有化MLOps；
- 定制化算法与驻场服务。

---

## 9. 12个月实施路线图

### Q1（MVP）
- 完成相机/机器人适配基础框架；
- 上线无序抓取最小闭环；
- 支持1个行业样板场景（汽车零部件）。

### Q2
- 增加拆码垛与上下料模板；
- 上线可视化流程编排；
- 完成首批3家标杆客户POC。

### Q3
- 引入仿真调试与离线验证；
- 建立数据闭环训练管道；
- 多品牌机械臂兼容增强。

### Q4
- 集团级运维平台上线；
- 发布企业版能力；
- 构建行业解决方案生态（系统集成商合作）。

---

## 10. 竞争差异化建议（相对“对标”）
1. **更低交付门槛**：模板化工艺+向导式部署，让非算法工程师也能落地；
2. **更强跨品牌兼容**：机器人、相机、PLC解耦；
3. **更快模型迭代**：失败样本自动闭环，按班次更新参数；
4. **更透明运维**：从“黑盒AI”升级到可解释指标与在线诊断。

> 建议先以1~2个高价值行业（如汽车零部件、3C）打穿，再横向复制到物流与新能源。
